# Specs

This directory is the source of truth for implementation order and current
state.

- `STATUS.md` captures the imported Otto status context used to seed this repo.
- `TODO_36_meeting_agent_poc.md` is the MeetingBaas meeting-agent PoC spec.
- `FIRST_INCREMENT_PLAN.md` tracks the first implementation sequence for this
  repo.

Update these files when architecture decisions, blockers, or next steps change.
